#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

set -e

cleanup() {
	./skills/tldraw-otel-lab/scripts/otel_lab.sh down >/dev/null 2>&1 || true
}
trap cleanup EXIT INT TERM

corepack yarn workspace @tldraw/sync test
corepack yarn workspace @tldraw/sync-core test

./skills/tldraw-otel-lab/scripts/otel_lab.sh up --fresh --no-worker
corepack yarn workspace @tldraw/dotcom-worker e2e-simple
